import 'package:flutter/material.dart';

/// All colors from player-frontend/src/index.css CSS variables.
class AppColors {
  // Background & Surface
  static const background = Color(0xFF0A0C12); // hsl(220, 20%, 4%)
  static const foreground = Color(0xFFFAFAFA); // hsl(0, 0%, 98%)
  static const card = Color(0xFF141822); // hsl(220, 15%, 8%)
  static const cardHover = Color(0xFF1E2230); // hsl(220, 15%, 12%)

  // Primary (Gold) – "Golden and Black" theme
  static const primary = Color(0xFFFFAA00); // hsl(42, 100%, 50%)
  static const primaryForeground = Color(0xFF0A0C12);

  // Secondary
  static const secondary = Color(0xFF1E2230); // hsl(220, 15%, 12%)
  static const secondaryForeground = Color(0xFFFAFAFA);

  // Accent (Cyan)
  static const accent = Color(0xFF22D3EE); // hsl(187, 85%, 53%)
  static const accentForeground = Color(0xFF0A0C12);

  // Muted
  static const muted = Color(0xFF262B38); // hsl(220, 15%, 15%)
  static const mutedForeground = Color(0xFF8C8C8C); // hsl(0, 0%, 55%)

  // Destructive
  static const destructive = Color(0xFFEF4444); // hsl(0, 84%, 60%)
  static const destructiveForeground = Color(0xFFFAFAFA);

  // Border & Input
  static const border = Color(0xFF2A2F3D); // hsl(220, 15%, 18%)
  static const input = Color(0xFF1E2230);
  static const ring = Color(0xFFFFAA00);

  // Casino colors
  static const casinoGold = Color(0xFFFFAA00);
  static const casinoOrange = Color(0xFFFF8C1A);
  static const casinoCyan = Color(0xFF22D3EE);
  static const casinoRed = Color(0xFFEF4444);
  static const casinoPurple = Color(0xFFAA44EE);
  static const casinoPink = Color(0xFFEE4488);
  static const casinoBlue = Color(0xFF0EA5E9);

  // Sidebar
  static const sidebarBackground = Color(0xFF0F1118); // hsl(220, 20%, 6%)
  static const sidebarPrimary = Color(0xFFFFAA00);
  static const sidebarAccent = Color(0xFF1E2230);

  // Success / Warning
  static const success = Color(0xFF22C55E);
  static const warning = Color(0xFFFACC15);
}
