import { CategoryGamePage } from '@/components/CategoryGamePage';

const GameShows = () => (
  <CategoryGamePage
    title="Game Shows"
    activeTab="game-shows"
    category="game-shows"
    emptyMessage="No game show games found"
  />
);

export default GameShows;
